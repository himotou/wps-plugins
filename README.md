# WPS 链接绑定插件

选中一段文字后点击功能区按钮，会弹出链接绑定窗口；如果当前选区已有超链接，会自动回填。没有选中文字时，只显示“未选择”。

## 用法

1. `npm install`
2. `npm run dev`
3. 在 WPS 里打开文档后选中文字
4. 点击“设置超链接”

## 说明

- 选中文本时，确认后直接给原文字写入超链接
- 已有超链接时，会把原链接带到弹框里
- 没有选中时，只显示“未选择”
